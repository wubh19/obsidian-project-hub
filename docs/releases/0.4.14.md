## 0.4.14

### Checklist

- [x] README.md
- [x] docs/DEPLOY.md
- [x] docs/RELEASE.md
- [x] Projects/obsidian-project-hub/**

### Added

- 新增 Community Plugins 设置页中的项目路径即时预览，可直接显示根层级容器下识别到的项目文件夹。

### Changed

- 项目路径配置统一以根层级容器目录为准，例如 `Projects`。
- 设置页简化为只维护项目路径，左侧工具栏继续使用默认看板图标。

### Fixed

- 修复新增路径时错误生成 `Projects/new-scope-*` 占位路径的问题。
- 修复新增项目后重新打开配置页仍看不到最新项目文件夹的问题。
- 修复旧配置中的 `Projects/new-scope-*` 占位路径未自动迁移的问题。

### Notes

- 历史配置中若存在 `Projects/new-scope-*`，插件加载时会自动按 `Projects` 迁移。
